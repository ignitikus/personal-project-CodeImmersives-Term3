import React from 'react'
import { connect } from 'react-redux'

export const MobilePiano = () => {
   return (
      <div>
         Under construction
      </div>
   )
}

const mapStateToProps = (state) => ({
   
})

export default connect(mapStateToProps, {})(MobilePiano)
